// printing T-shirts
function make_shirt(size: string, message: string): void {
    console.log(`A ${size}-sized shirt would  print : "${message}".`);
}

// Call the function
make_shirt("Medium", "Compiler pani main gaya , Chapak Chapak Chapak");