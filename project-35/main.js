var favanimals = ["Snakes", "Black Panthers", "Lion"];
// Name of each animal
console.log("Favourite animal names:");
for (var _i = 0, favanimals_1 = favanimals; _i < favanimals_1.length; _i++) {
    var animal = favanimals_1[_i];
    console.log(animal);
}
// Line for every animal
console.log("\nLine for every animal:");
for (var _a = 0, favanimals_2 = favanimals; _a < favanimals_2.length; _a++) {
    var animal = favanimals_2[_a];
    console.log(" ".concat(animal.toLowerCase(), " are dangerous and wild animals."));
}
// Special line
console.log("\nAll of them are my most favourite because everyone get scared from them easily.");
