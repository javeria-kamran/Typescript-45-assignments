// Version 1 (Passing the if test) in TypeScript:

let alienColor: string = 'red';

// If statement ,to test whether the alien's color is red
if (alienColor === 'red') {
    console.log("Player just earned 5 points.");
}

// Version 2 (Failing the if test) in TypeScript (No-Output)
let Alien2Color: string = 'green';

// If statement to test whether the alien's color is red
if (Alien2Color === 'red') {
    console.log("Player just earned 5 points.");
}